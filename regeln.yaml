rules:
  - id: avoid-hardcoded-secrets
    message: "Vermeide hartcodierte Geheimnisse wie API-Schlüssel."
    severity: WARNING
    patterns:
      - pattern: |
          $KEY = "hardcoded_value"
    languages:
      - java

